import styled from 'styled-components'

import theme from '@/theme'

export const ErrorContainer = styled.div`
  color: ${theme.colors.error};
  font-size: ${theme.textStyle.xl.fontSize};
  line-height: ${theme.textStyle.xl.lineHeight};
  margin: 100px auto;
  text-align: center;
`
